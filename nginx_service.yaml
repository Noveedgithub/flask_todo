apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: LoadBalancer  # Expose the Nginx service externally
  selector:
    app: nginx
  ports:
  - protocol: TCP
    port: 80       # External port to expose
    targetPort: 80 # Nginx container's port
